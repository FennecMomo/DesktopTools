# 项目提交与发布约定

- 每次 Git 提交都按本次提交的最高级别变更递增 `A.B.C` 中的一位：系统框架或不兼容架构调整递增 `A`；新增、扩展功能模块递增 `B`；修复、一般修改、界面细节或文档递增 `C`。递增高位时，右侧数字归零。版本号按提交次数计算，不按一次提交里包含的功能数量计算。
- 提交前同步更新 `overlay_window.pyw` 的 `APP_VERSION`，重新打包 `dist/DesktopTools.exe` 并生成相匹配的 `dist/update.json`；源码、EXE 和索引在同一次提交中保持一致。
- Git 提交日志使用中文，必须包含本次版本号和具体变更内容，例如 `功能版本 2.2.0：新增浏览器与快捷隐藏功能`。不能只写“更新”或只写版本号。
- 用户说“推送”时，表示完成提交并推送；不要因此自动创建 GitHub Release。只有用户明确要求“发布版本”或“发布 Release”时才创建发布。
- 用户没有要求提交或推送时，不擅自提交、推送或发布。
